services:
  api:
    build: .
    ports: ["8080:8080"]
    environment:
      MODEL_CACHE_DIR: /app/.model_cache
    restart: unless-stopped
    volumes:
      - model_cache:/app/.model_cache

volumes:
  model_cache:
